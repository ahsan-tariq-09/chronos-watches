{
  "name": "chronos-watches",
  "private": true,
  "version": "3.0.0",
  "description": "Chronos Watches - FastAPI backend with React + TypeScript frontend",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "cd backend && . .venv/bin/activate 2>/dev/null || true; uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "test": "npm run test -w frontend && cd backend && . .venv/bin/activate 2>/dev/null || true; pytest",
    "install:frontend": "npm install -w frontend",
    "install:backend": "python3 -m venv backend/.venv && backend/.venv/bin/pip install -r backend/requirements.txt"
  }
}
